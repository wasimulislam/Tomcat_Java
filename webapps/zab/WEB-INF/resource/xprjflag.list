     list xprjflag
        table xcodes
        order xtype,xcode
        select "((xcode like '%"+zvalue+"%' or xlong like '%"+zvalue+"%') and xtype='Project Flag')"
        rows 20
        objects xcode attrib(link "abc" embed onclick="return updateParent(this:'')"), ~
                xlong
     end list


