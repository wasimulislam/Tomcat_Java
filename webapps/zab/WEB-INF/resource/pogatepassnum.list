 list pogatepassnum
        table poreqheader
        order xporeqnum desc
        select "xporeqnum like '%"+zvalue+"%' and left(xporeqnum,4) in ('GP--')"
		searchselect "left(xporeqnum,4) in ('GP--')"
        rows 20
        objects xporeqnum attrib(link "abc" embed onclick="return updateParent(this:'')"),~
                xfwh,xdate

     end list


