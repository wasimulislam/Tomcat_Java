     list mobatch
		caption "Batch No"
        table moheader
        order xbatch desc
        select "(xbatch like '%"+zvalue+"%')"
        rows 20
        objects xbatch attrib(link "abc" embed onclick="return updateParent(this:'')"), ~
                xmodate,xqtymor,xpiref

        //totals "","Totals","","","","",sum,sum

        //headers none 
     end list


